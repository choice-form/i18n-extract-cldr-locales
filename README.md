# i18n-extract-cldr-locales

Extract locales display names from **cldr-localenames-modern** repo.

## How to use

Run this command to extract locales

```bash
npm run build
```

The locale names are generated under `locales/` folder.
